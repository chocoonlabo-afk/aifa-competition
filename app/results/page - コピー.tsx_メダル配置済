import type { CSSProperties } from "react";

const winners = [
  {
    age: 11,
    name: "Lena Kuczyńska",
    country: "Poland",
    flagSrc: "/flags/pl.svg",
    medalSrc: "/medals/age-11.png",
  },
  {
    age: 10,
    name: "Mason Yuan",
    country: "United States",
    flag: "🇺🇸",
    medalSrc: "/medals/age-10.png",
  },
  {
    age: 9,
    name: "Wang Tinuo",
    country: "China",
    flag: "🇨🇳",
    medalSrc: "/medals/age-9.png",
  },
  {
    age: 7,
    name: "Alisa Ivić",
    country: "Germany",
    flag: "🇩🇪",
    medalSrc: "/medals/age-7.png",
  },
  {
    age: 5,
    name: "Amane Yago",
    country: "Japan",
    flag: "🇯🇵",
    medalSrc: "/medals/age-5.png",
  },
];

export default function ResultsPage() {
  const wrapper: CSSProperties = {
    minHeight: "calc(100vh - 64px)",
    padding: "40px 16px 80px",
    boxSizing: "border-box",
    display: "flex",
    justifyContent: "center",
  };

  const card: CSSProperties = {
    width: "100%",
    maxWidth: "900px",
    background:
      "radial-gradient(circle at 0% 0%, rgba(129,140,248,0.2), transparent 60%), #020617",
    borderRadius: "18px",
    padding: "28px 28px 32px",
    boxShadow: "0 18px 45px rgba(15,23,42,0.65)",
    border: "1px solid rgba(148,163,184,0.45)",
  };

  const title: CSSProperties = {
    fontSize: "24px",
    fontWeight: 700,
    margin: "0 0 18px",
    color: "#fefce8",
  };

  const p: CSSProperties = {
    margin: "0 0 14px",
    fontSize: "14px",
    lineHeight: 1.8,
    color: "#e5e7eb",
  };

  const yearBlock: CSSProperties = {
    marginTop: "22px",
    padding: "18px 20px",
    borderRadius: "14px",
    border: "1px solid rgba(148,163,184,0.7)",
    background: "rgba(2,6,23,0.9)",
  };

  const yearTitle: CSSProperties = {
    margin: "0 0 12px",
    fontSize: "17px",
    fontWeight: 700,
    color: "#fefce8",
  };

  const winnerList: CSSProperties = {
    display: "grid",
    gap: "12px",
    margin: "18px 0 20px",
  };

  const winnerItem: CSSProperties = {
    display: "flex",
    alignItems: "center",
    gap: "14px",
    padding: "12px 14px",
    borderRadius: "12px",
    border: "1px solid rgba(250,204,21,0.45)",
    background:
      "linear-gradient(90deg, rgba(250,204,21,0.09), rgba(15,23,42,0.85))",
  };

  const medal: CSSProperties = {
    width: "84px",
    height: "84px",
    objectFit: "contain",
    flexShrink: 0,
    filter: "drop-shadow(0 6px 10px rgba(0,0,0,0.35))",
  };

  const winnerText: CSSProperties = {
    flex: 1,
    minWidth: 0,
  };

  const division: CSSProperties = {
    fontSize: "13px",
    fontWeight: 700,
    color: "#facc15",
    marginBottom: "4px",
  };

  const name: CSSProperties = {
    fontSize: "15px",
    fontWeight: 700,
    color: "#ffffff",
  };

  const country: CSSProperties = {
    fontSize: "13px",
    color: "#cbd5e1",
    marginTop: "2px",
  };

  const quote: CSSProperties = {
    margin: "18px 0",
    padding: "14px 16px",
    borderLeft: "3px solid #facc15",
    background: "rgba(250,204,21,0.08)",
    color: "#fefce8",
    fontSize: "14px",
    lineHeight: 1.8,
    fontStyle: "italic",
  };

  const small: CSSProperties = {
    fontSize: "12px",
    color: "#9ca3af",
    marginTop: "18px",
    lineHeight: 1.7,
  };

  return (
    <main style={wrapper}>
      <section style={card}>
        <h1 style={title}>Winners &amp; Results</h1>

        <p style={p}>
          We are delighted to announce that the inaugural AIFA International
          Music Competition has successfully concluded.
        </p>

        <p style={p}>
          Despite being our very first edition, we received applications from
          highly dedicated young violinists from around the world, resulting in
          an exceptionally high standard of performances and adjudication.
        </p>

        <div style={yearBlock}>
          <h2 style={yearTitle}>2026 Inaugural Grand Prix Winners</h2>

          <p style={p}>
            After careful and rigorous evaluation, the following participants
            were awarded the Grand Prix in each age division:
          </p>

          <div style={winnerList}>
            {winners.map((winner) => (
              <div key={winner.age} style={winnerItem}>
                <img
                  src={winner.medalSrc}
                  alt={`Age ${winner.age} Division Grand Prix medal`}
                  style={medal}
                />

                <div style={winnerText}>
                  <div style={division}>
                    Age {winner.age} Division Grand Prix
                  </div>
                  <div style={name}>{winner.name}</div>
                  <div style={country}>
                    {winner.flag} {winner.country}
                  </div>
                </div>
              </div>
            ))}
          </div>

          <p style={p}>
            In addition, recipients of other awards are being notified
            individually. Certificates and written evaluations from the jury are
            currently being prepared and will be sent out sequentially. We
            kindly ask all participants for a little more patience while we
            complete the process.
          </p>

          <div style={quote}>
            “Even through an online format, each performance conveyed remarkable
            passion, sincerity, and musical expression through every single
            note.”
          </div>

          <p style={p}>
            The AIFA International Music Competition is committed to supporting
            the next generation of musicians through a fair, encouraging, and
            educational environment.
          </p>

          <p style={p}>
            Preparations for the next edition are already underway, and further
            details will be announced soon.
          </p>

          <p style={p}>
            We would like to express our heartfelt gratitude to all participants
            and supporters who made this memorable first competition possible.
          </p>

          <p style={small}>
            Assessment / Improvement / Fairness / Achievement
          </p>
        </div>
      </section>
    </main>
  );
}